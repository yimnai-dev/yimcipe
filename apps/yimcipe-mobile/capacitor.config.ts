import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'yimcipe-mobile',
  webDir: '../../dist/apps/yimcipe-mobile',
  bundledWebRuntime: false,
};

export default config;
